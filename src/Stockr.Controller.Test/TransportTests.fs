module TransportTests

open Xunit
open FsUnit

[<Fact>]
let ``Converting a transport spec to a transport must yield the correct Measure for the quantity`` () =
     ()
    
[<Fact>]
let ``Adding all quantities must return sum of all transport quantities`` () =
     ()